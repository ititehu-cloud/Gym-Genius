
{
  "entities": {
    "UserProfile": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "User Profile",
      "type": "object",
      "description": "Represents a user's profile.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the user, matching Firebase Auth UID."
        },
        "displayName": {
          "type": "string",
          "description": "The user's display name."
        },
        "email": {
          "type": "string",
          "description": "The user's email address.",
          "format": "email"
        },
        "phoneNumber": {
          "type": "string",
          "description": "The gym's contact phone number."
        },
        "displayAddress": {
          "type": "string",
          "description": "The gym's display address."
        },
        "icon": {
          "type": "string",
          "format": "uri",
          "description": "URL for the user's profile icon, typically the gym logo."
        }
      },
      "required": ["id", "displayName", "email"]
    },
    "Attendance": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Attendance",
      "type": "object",
      "description": "Represents a member's attendance record.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the attendance record."
        },
        "userId": {
          "type": "string",
          "description": "The UID of the gym owner who owns this record."
        },
        "memberId": {
          "type": "string",
          "description": "Reference to Member."
        },
        "checkInTime": {
          "type": "string",
          "description": "Timestamp when the member checked in.",
          "format": "date-time"
        },
        "checkOutTime": {
          "type": "string",
          "description": "Timestamp when the member checked out.",
          "format": "date-time"
        }
      },
      "required": [
        "userId",
        "memberId",
        "checkInTime"
      ]
    },
    "Payment": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Payment",
      "type": "object",
      "description": "Represents a payment made by a member.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the payment."
        },
        "userId": {
          "type": "string",
          "description": "The UID of the gym owner who owns this record."
        },
        "memberId": {
          "type": "string",
          "description": "Reference to Member."
        },
        "paymentDate": {
          "type": "string",
          "description": "Date when the payment was made.",
          "format": "date-time"
        },
        "amount": {
          "type": "number",
          "description": "Amount paid by the member."
        },
        "paymentMethod": {
          "type": "string",
          "description": "Payment method used (e.g., cash, credit card)."
        },
        "paymentType": {
          "type": "string",
          "description": "The type of payment.",
          "enum": [
            "monthly",
            "renewal",
            "advance"
          ]
        },
        "invoiceNumber": {
          "type": "string",
          "description": "Invoice number for the payment."
        },
        "status": {
          "type": "string",
          "description": "The payment's current status.",
          "enum": [
            "paid",
            "pending"
          ]
        }
      },
      "required": [
        "userId",
        "memberId",
        "paymentDate",
        "amount",
        "paymentMethod",
        "paymentType",
        "status"
      ]
    },
    "Plan": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Plan",
      "type": "object",
      "description": "Represents a gym membership plan.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the plan."
        },
        "userId": {
          "type": "string",
          "description": "The UID of the gym owner who owns this plan."
        },
        "name": {
          "type": "string",
          "description": "Name of the plan."
        },
        "description": {
          "type": "string",
          "description": "Description of the plan."
        },
        "price": {
          "type": "number",
          "description": "Price of the plan."
        },
        "duration": {
          "type": "number",
          "description": "Duration of the plan in months."
        }
      },
      "required": [
        "userId",
        "name",
        "price",
        "duration"
      ]
    },
    "Member": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Member",
      "type": "object",
      "description": "Represents a gym member.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the member."
        },
        "userId": {
          "type": "string",
          "description": "The UID of the gym owner who owns this member."
        },
        "memberId": {
          "type": "string",
          "description": "The member's custom display ID."
        },
        "name": {
          "type": "string",
          "description": "The member's full name."
        },
        "mobileNumber": {
          "type": "string",
          "description": "The member's mobile phone number."
        },
        "address": {
          "type": "string",
          "description": "The member's physical address."
        },
        "planId": {
          "type": "string",
          "description": "Reference to the selected Plan."
        },
        "joinDate": {
          "type": "string",
          "description": "The date the member joined.",
          "format": "date-time"
        },
        "expiryDate": {
          "type": "string",
          "description": "The date the membership expires.",
          "format": "date-time"
        },
        "imageUrl": {
          "type": "string",
          "description": "URL of the member's profile picture.",
          "format": "uri"
        },
        "status": {
          "type": "string",
          "description": "The member's current status.",
          "enum": [
            "active",
            "expired",
            "due"
          ]
        }
      },
      "required": [
        "userId",
        "memberId",
        "name",
        "address",
        "planId",
        "joinDate",
        "expiryDate",
        "imageUrl",
        "status"
      ]
    },
    "MemberNote": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Member Note",
      "type": "object",
      "description": "Represents a historical note about a gym member.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the note."
        },
        "userId": {
          "type": "string",
          "description": "The UID of the gym owner who owns this note."
        },
        "memberId": {
          "type": "string",
          "description": "Reference to Member."
        },
        "content": {
          "type": "string",
          "description": "The text content of the note."
        },
        "noteDate": {
          "type": "string",
          "description": "Timestamp for the note.",
          "format": "date-time"
        }
      },
      "required": [
        "userId",
        "memberId",
        "content",
        "noteDate"
      ]
    }
  },
  "auth": {
    "providers": [
      "password"
    ]
  },
  "firestore": {
    "/users/{userId}": {
      "schema": {
        "$ref": "#/entities/UserProfile"
      },
      "description": "Stores user profile data."
    },
    "/attendance/{attendanceId}": {
      "schema": {
        "$ref": "#/entities/Attendance"
      },
      "description": "Stores attendance records for members."
    },
    "/payments/{paymentId}": {
      "schema": {
        "$ref": "#/entities/Payment"
      },
      "description": "Stores payment information for members."
    },
    "/plans/{planId}": {
      "schema": {
        "$ref": "#/entities/Plan"
      },
      "description": "Stores gym membership plans."
    },
    "/members/{memberId}": {
      "schema": {
        "$ref": "#/entities/Member"
      },
      "description": "Stores gym member profiles."
    },
    "/notes/{noteId}": {
      "schema": {
        "$ref": "#/entities/MemberNote"
      },
      "description": "Stores personal notes about gym members."
    }
  }
}
